const LoadMoreButton = ({ onLoadMoreButtonClick }) => {
    return (
        <button type="button" className="Button" onClick={onLoadMoreButtonClick}>Load more</button>
    )
}

export default LoadMoreButton